DEFAULT_HOST = "the ip address of the local SQL Server"
DEFAULT_ODBC_DRIVER = "the preferred ODBC driver" # "SQL Server" will do in most cases
DEFAULT_DATA_CATALOG = "the database holding the data table where I want to start working" # E.g. "dbo.my_schema"
DEFAULT_DATA_TABLE = "the table holding the data where I want to start working" # E.g. "iris"
DEFAULT_CLASSIFICATION_CATALOG = "the database where to store the classification results" # E.g. "dbo"
DEFAULT_CLASSIFICATION_TABLE = "the table where to store the classification results" # E.g. "Autoclassification"
